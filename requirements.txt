fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-dotenv>=1.0.0
requests>=2.31.0
SQLAlchemy>=2.0.0
psycopg[binary]>=3.2.0
alembic>=1.13.0
celery>=5.4.0
redis>=5.0.0
weasyprint>=60.0
markdown>=3.5.0
stripe>=7.0.0
# Nmap integration (system dependency: nmap must be installed)
# python-nmap is optional, we use subprocess for better control
# Dependency scanning uses httpx (already included)
